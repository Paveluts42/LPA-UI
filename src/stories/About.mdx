
export const Version=JSON.stringify(require("../../package.json").version);
export const Name=JSON.stringify(require("../../package.json").name);

# {Name}  Версия : {Version}
<br/>

## Посделние изменения

<div style={{display:'block'}}>
    <b>
        Версия "1.0.0003" :
    </b>
    <b>Добавлена документация </b>

    <b>Добавлены иконки:</b> Add', 'ArrowLeft', 'ArrowRight', 'ArrowUp', 'Done', 'Edit', 'Filter', 'Search', 'Settings',

    <b>Добавлен компонент настройки темы:</b>  ThemeProvider,useTheme  доступны 2 темы light | dark
</div>

