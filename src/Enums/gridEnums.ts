export enum GridGap {
  small = 8,
  medium = 16,
  large = 24,
  none = 0,
}
export enum GridAutoFlow {
  row = 'row',
  column = 'column',
  dense = 'dense',
  rowDense = 'row dense',
  columnDense = 'column dense',
}
